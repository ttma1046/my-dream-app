import { LearnPipePipe } from './learn-pipe.pipe';

describe('LearnPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LearnPipePipe();
    expect(pipe).toBeTruthy();
  });
});
