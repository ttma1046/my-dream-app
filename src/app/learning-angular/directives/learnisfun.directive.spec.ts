import { LearnisfunDirective } from './learnisfun.directive';

describe('LearnisfunDirective', () => {
  it('should create an instance', () => {
    const directive = new LearnisfunDirective();
    expect(directive).toBeTruthy();
  });
});
